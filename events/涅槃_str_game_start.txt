namespace = str_game_start
### Hidden Game Start Event
country_event = {
	id = str_game_start.0
	hide_window = yes
	is_triggered_only = yes
	trigger = { 
		is_ai = yes 
		NOT = { has_country_flag = has_str_ai_modifier } 
	}
	immediate = { 
		add_modifier = { modifier = str_ai_modifier } 
		if = {
			limit = { NOT = { is_country_type = default } }
			add_modifier = { modifier = str_ai_modifier_ship }
		}
		set_country_flag = has_str_ai_modifier 
	}
}