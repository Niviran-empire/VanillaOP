id = $ID$
title = $ID$.title
desc = $ID$.desc
picture = GFX_evt_society_cache
show_sound = event_scanner
is_advisor_event = yes
is_triggered_only = yes
trigger = {
	has_global_flag = EOF_NOVA_SINGULARY_enable
	exists = event_target:global_event_country
	event_target:global_event_country = { OR = {
		check_modifier_value = { modifier = nova_mod_order_check value > 0 }
		check_modifier_value = { modifier = kuat_mod_order_check value > 0 }
	} }
}
immediate = { 
	change_variable = { which = alert_system_amount value = 1 } 
	if = {
		limit = { check_variable = { which = alert_system_amount value > 3 } }
		random_list = {
			3 = { random_list = {
				5 = { random_owned_ship = { ship_event = { id = $TARGET$ } } }
				5 = { }
			} }
			9 = {  }
		}
		reroll_random = yes
	}
}
option = { name = $ID$.a }