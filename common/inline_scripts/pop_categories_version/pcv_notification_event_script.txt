id = $ID$
title = $ID$.title
desc = $ID$.desc
picture = GFX_evt_society_cache
show_sound = event_scanner
is_advisor_event = yes
mean_time_to_happen = { days = 30 }
trigger = { str_debug_check_trigger = yes }
immediate = { 
	change_variable = { which = alert_system_amount value = 1 } 
	if = {
		limit = { check_variable = { which = alert_system_amount value > 3 } }
		random_list = {
			3 = { random_owned_ship = { ship_event = { id = $TARGET$ } } }
			9 = {  }
		}
		reroll_random = yes
	}
}
option = { name = $ID$.a }