id = $ID$
hide_window = yes
is_triggered_only = yes
immediate = { 
	inline_script = {
		script = pop_categories/pop_cate_upkeep/resource_global_$TARGET$_$FLAG$
		TARGET = $TARGET$ FLAG = $FLAG$ PREV = $PREV$
	}
	every_system = {
		remove_star_flag = can_enable_can_be_system_blocks_sensors
		if = {
			limit = { OR = {
				##_|星云|	+RS/Sexp
				is_inside_nebula = yes
				has_modifier = space_storm
				##_|封印星系|	+Sexp
				has_star_flag = lcluster
				has_star_flag = sealed_system
			} }
			set_star_flag = can_enable_can_be_system_blocks_sensors
		}
	}
}